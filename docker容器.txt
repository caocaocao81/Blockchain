dockecurl -X POST -H "Content-Type: application/json" -d '{"sender": "891a9c1f3d1a4575871eaa2f2e44b85e","recipient": "mymymyacount","amount": 5}' "http://115.199.70.15:12381/transactions/new"

docker run --rm  -p 12381:5001 --net blockchain --name node1  --ip 172.19.0.2      blockchain

docker run --rm  -p 12381:5030 --net visualserver --name visualserver -v /C/Users/cao/Desktop/runoob01/blockchain/Readfile:/app --ip 172.20.0.2     visualserver

docker run -it --rm -p 12382:3965 --net visualserver --name node1 -v /C/Users/cao/Desktop/runoob01/blockchain/Readfile:/app --ip 172.20.0.3 node

docker exec -it containerID /bin/bash

docker run --rm  -p 5030:5030 --net visualserver --name visualserver -v /app:/app --ip 172.20.0.2     visualserver

docker run -it --rm -p 3965:3965 --net visualserver --name node1 -v /app:/app --ip 172.20.0.3 node

docker run --rm -it  --net visualserver --name node1 -v /home/block_node:/app --ip 172.20.0.2 node

docker run --rm  -p 8000:8000 -p 5030:5030 --net visualserver --name visualserver -v /app:/app --ip 172.20.0.3     visualserver

容器id：b9b11b2b45f8

先从cmd ipconfig中查看本机地址 然后让区块链私链依据本地ip地址启动 然后再在容器中启动测试节点，然后再进行挖矿操作验证是否可以实现保存读取文件。

在执行docker run的时候如果添加--rm参数，则容器终止后会立刻删除。--rm参数和-d参数不能同时使用。

手机热点的ip：192.168.43.188

先启动visualserver的节点，（目前）然后再在该容器中启动其他的节点（启动前先在本机打开区块链）注意：得配置好区块链连接的ip，其ip地址必须是本机的ip，不然会导致无法连接（因为这里本机ip并不固定，所以有时候操作比较繁琐，可以写个for循环把会用到的ip地址都放入其中然后循环调用如果成功了就直接跳出循环）

接下来实现，把节点部署到其他容器，然后进行容器直接互通，再实现信息互动。

节点互通完成...但是保存的数据不知道保存到哪里去了....